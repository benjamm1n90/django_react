server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # React Router uses client-side routing, so unknown paths should fall
    # back to index.html instead of a raw nginx 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
